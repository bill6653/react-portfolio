import React from "react";

const Resume = () => {
  return (
    <section className="section section-resume">
    <div className="section-resume__content container">
      <h2>This is the page for my resume when i upload it.</h2>
    </div>
  </section>
  );
};

export default Resume;
